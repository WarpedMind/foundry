# Foundry Session Summary
# Entries are ordered newest-to-oldest. Most recent session is at the top.
# Entries older than the newest two are archived, verbatim, in SESSIONS_ARCHIVE.md
# (starting from "2026-08-18 (Absolute-rule candidate triage)")
# — not auto-loaded; see "Where the history lives" in CLAUDE.md.

## 2026-08-18 (Doc-budget handoff item 4 — the durable half)

Built the mechanism the previous session's one-time cut left missing, closing `PROPOSALS/2026-08-18-doc-budget-handoff.md` entirely.

**Verified the loaded set rather than assuming it.** The handoff and `audit.sh`'s check 9 both treated the auto-loaded docs as a hardcoded `CLAUDE.md DECISIONS.md SESSIONS.md`. Reading the actual rendered hook command showed why that is wrong in general: `templates/settings.hooks.json.template` renders `{{DOC_FILES_QUOTED}}`, so the list is per-project. Two corrections fell out. The set is now parsed from `.claude/settings.json`'s real `DOC_FILES_ARR=(...)` (textually — `audit.sh` has no `jq` dependency, and the array lives inside a JSON string so its quotes arrive escaped). And `CLAUDE.md` is unioned in unconditionally, because Claude Code loads it as project instructions with or without a hook — which is why "no SessionStart hook" is *not* an N/A here. Check 9 was rewired onto the same derived set in the same change; its old hardcoded list ran in the direction of a false pass, silently excluding documents the session never sees.

**Part (a) — the check, mutation-tested.** Check 11, `auto-load budget`. INFO at 160,000 B, FAIL at a 304,000 B ceiling, threshold on the total with the per-file breakdown printed on every outcome including PASS. Both numbers are defended in `skills/foundry-audit/SKILL.md` and `DECISIONS.md`: the budget from two independent derivations that were not adjusted to agree (20% of a 200k window at this repo's measured 4.0 bytes/token → 160,000 B; and ~76,000 B of headroom over the post-archive baseline ≈ five average sessions at the +14,700 B/session rate measured across all 51 commits of this repo's history), the ceiling from no derivation at all — it is the 303,867-byte level this repo actually reached and a recorded decision actually rejected. Negative branches: a loader present but unparseable is a **SKIP** (a finding — the size is unknown, not fine), clearable with a new `--budget-file` flag so the strict default can never become permanent unclearable noise; only a project with no `CLAUDE.md` *and* no loader is N/A.

**8 new mutation cases** (30 → 38), and each was confirmed capable of failing rather than assumed: budget crossed (asserting exit **0** as well as the text — an implementation that made it a FAIL would print the right message and behave wrongly), ceiling crossed, an oversized doc *outside* the loaded set staying quiet, the unparseable SKIP, `--budget-file` clearing it, a loader naming a missing file, and a matched pair for check 9's rewiring. Then three separate neuterings of `audit.sh` — thresholds made unreachable, check 9 reverted to its hardcoded list, the unparseable branch removed — each turned exactly the expected cases red and nothing else. One case initially passed vacuously (the injected rule read `Never` capitalized; check 9's pattern is case-sensitive), caught by the neuter pass rather than by review.

**Part (b) — the discipline, prose, and labeled as such.** `foundry.docBudgetDiscipline` in `foundry-repo-hygiene` Part 2, same shape as `foundry.readmeChangelogDiscipline` including its `"not-applicable-yet"` state, asked the first time the budget check reports INFO/FAIL rather than at init time. All four negative branches written out by name: never fires (freshness check writes `"not-applicable-yet"`); fires before the field was asked (**means ask now, never `"off"`** — absence of an answer is not a recorded decision to skip); `"off"` chosen (scheduling preference only — the ceiling FAIL still fails); and `foundry-audit` never run at all, which has no mechanism behind it by construction and says so rather than being papered over. The entry states plainly that no case in `tests/run_fixtures.sh` covers any of it and none can.

**Also recorded four refusals** — no second "approaching budget" threshold, no tokenizer dependency, no Hook 3 periodic nudge or auto-archive, and no budget check inside the SessionStart hook itself — because each is cheap enough to look like thoroughness to a later session.

**Four drift points the change itself created, found by checking rather than assumed absent.** `templates/CLAUDE.md.template`'s archive rule — which the previous session deliberately kept verbatim-identical to this repo's copy so the two could not drift — said "when these docs do grow large enough to archive" and named no mechanism for knowing when that is, which is exactly the gap item 4 filled; the budget-check sentence was added there too. And three places enumerate the audit's checks outside its own SKILL.md (`skills/foundry-help/SKILL.md`, `skills/foundry-init/SKILL.md` Step 3, `USER_GUIDE.md`), each of which would have shipped an incomplete list. None of these are caught by `audit.sh` (it doesn't scan `skills/`, and an incomplete prose list isn't a structural defect) — found by grepping for who else describes this check, which is the habit that matters here rather than the tool.

**Verification categories, kept separate on purpose.** Part (a) is mechanical and mutation-tested, including the three neuter experiments proving the harness can go red. Part (b) is prose judgment with no artifact to inject a defect into; the green suite says nothing about it. `bash skills/foundry-audit/audit.sh` and `bash tests/run_fixtures.sh` both clean.

## 2026-08-18 (Doc-budget handoff items 1-3 — `PROPOSALS/2026-08-18-doc-budget-handoff.md`)

Executed the mechanical part of the handoff written earlier the same day: archive verbatim, cut nothing. Re-derived every boundary command against the real files rather than trusting the handoff's numbers, since the repo had grown since it was measured (CLAUDE.md 61,737 → 64,843 bytes; DECISIONS.md 82,394 → 86,443 by the time this session started). All three boundaries matched the handoff's predicted line numbers exactly (CLAUDE.md `START=29 END=70`; SESSIONS.md cut at line 84; DECISIONS.md cut at line 195) even though byte counts had drifted — the section boundaries themselves were stable.

**Item 1.** Moved CLAUDE.md's 46KB `Current status` narrative log (Sessions 4-20) verbatim to a new `PROJECT_HISTORY.md`, verified with `grep -c 'Session 4'` (2 hits survived) and a byte-count diff. Wrote a fresh 15-25 line `Current status` in its place, answering only what's true now, not the session-by-session history — the one deliberately lossy step in the whole handoff, with the full original sitting beside it in `PROJECT_HISTORY.md`. Added the "Where the history lives" pointer block and the matching archive Rule (copied verbatim from `templates/CLAUDE.md.template`, per the handoff's instruction not to retype it).

**Item 2.** Split SESSIONS.md at the third `## 20` heading. Checked the handoff's "keep newest three" instinct against real numbers before applying it: the 2026-08-18 entries are long enough that three would leave ~70KB inline, defeating most of the point, so **kept two instead** (22,453 bytes inline, decided and stated as a cutover rule in both the file's own header and a new DECISIONS.md entry — this measured-not-assumed correction is itself now part of the archive plan so a future session extends it rather than re-litigating it).

**Item 3.** Split DECISIONS.md at the first `## 2026-06` heading — 34,934 bytes archived, 2026-08 entries (actively load-bearing, cited directly by name in several SKILL.md files) kept inline. This was the highest cross-reference-risk item, as the handoff warned: `foundry-audit`'s decision-reference check immediately caught **6 real dangling references** to now-archived decisions (Hook 3's three-state design, the `PostToolUse` auto-run rejection) — three in CLAUDE.md, two in DECISIONS.md itself (matched because the audit tags every line by source file, so any bare ISO date anywhere in DECISIONS.md's own prose is treated as a reference needing a local heading — a real quirk worth knowing before the next archive split), one in STACK.md. All six fixed by pointing to `DECISIONS_ARCHIVE.md` explicitly (with a section name, not a bare date, for the two inside DECISIONS.md itself, since a bare date there re-triggers the same check regardless of which archive file is named). `skills/foundry-hooks/SKILL.md`, `skills/foundry-help/SKILL.md`, and `skills/foundry-audit/SKILL.md` had the same dangling pattern (not audited — `audit.sh` doesn't scan `skills/`) and were fixed for consistency, found by grepping for the same date string across the repo rather than waiting for a future audit expansion to catch them.

**Result:** `CLAUDE.md` + `DECISIONS.md` + `SESSIONS.md` combined dropped from 303,867 bytes (~76,000 tokens, the measured baseline) to **83,785 bytes (~21,000 tokens) — a 72.4% cut**, better than the handoff's ~93KB projection despite this session adding new prose (the fresh `Current status`, the pointer block, a new DECISIONS.md entry recording the cutover rules, and this entry itself) — the newly-written material came in tighter than what it replaced, and this session's own SESSIONS.md entries were kept to the same two-entry inline window as everything else, so the file didn't grow past its own new rule. `bash skills/foundry-audit/audit.sh` and `bash tests/run_fixtures.sh` both clean after every step, not just at the end.

**Item 4 (the mechanical size check in `foundry-audit` plus the `foundry-repo-hygiene` discipline entry) was explicitly not started**, per both the handoff's own model-sizing guidance and the user's instruction to stop before it — it needs a different kind of session (design work against this repo's specific bar), not a continuation of today's mechanical moves.

No SESSIONS.md entry was written for the archive-boundary correction (item 2's "two, not three") as a separate finding — it's folded into this entry since it happened as part of executing item 2, not as a discovery afterward. A DECISIONS.md entry was added for all three cutover rules together (CLAUDE.md's rewrite scope, SESSIONS.md's two-entry window, DECISIONS.md's no-further-cut decision) — see its 2026-08-18 "Doc-budget handoff items 1-3 executed" entry.

